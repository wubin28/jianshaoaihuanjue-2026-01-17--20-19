## Me:

```markdown
我都认同。ENTER SPEC-PLAN MODE
```

## Claude code with sonnet 4.5:

```markdown

● [MODE: SPEC-PLAN]

  ---
  SPEC-PLAN

  1. SPEC：经过澄清的需求（What & Why）

  产品名称：减少AI幻觉 - 提示词优化工具

  核心功能：
  - 用户输入原始提示词
  - 点击"查询常识"按钮后，系统自动在原始提示词前后添加"减幻提示词"
  - 生成的优化提示词显示在输出区域，支持一键复制
  - 输入框自动清空，便于继续输入新内容

  减幻提示词格式：
  你是专家。

  [用户原始提示词]

  请提供主要观点的3个不同出处的网页链接以便我查验。如果你不知道或查不到，就实说，不要编造。

  界面要素：
  - 主标题："减少AI幻觉"
  - 输出区域：位于上方，可滚动查看，替换模式（只显示最新），带复制按钮
  - 输入区域：多行文本框 + 右侧"查询常识"按钮
  - 橙色主题视觉风格
  - 响应式适配电脑与手机屏幕

  ---
  2. PLAN：经过确认的技术实现方案（Tech Stack）
  ┌──────────┬──────────────────────────────────────────────────┐
  │   层面   │                       方案                       │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 框架     │ Next.js 16 App Router（保持现有技术栈）          │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 组件     │ React 19 函数组件 + useState 管理状态            │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 样式     │ CSS Modules（globals.css + page.module.css）     │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 布局     │ 垂直堆叠：输出区 → 输入区（输入框+按钮水平排列） │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 主题色   │ 橙色作为点缀色（按钮、标题），白色/浅灰背景      │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 响应式   │ 桌面优先，640px 断点切换移动端布局               │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 复制功能 │ navigator.clipboard API，按钮文字反馈"已复制"    │
  ├──────────┼──────────────────────────────────────────────────┤
  │ 交互     │ 客户端组件（'use client'），纯前端逻辑           │
  └──────────┴──────────────────────────────────────────────────┘
  ---
  下一步：你已完成一次完整的'RI-SP'提示词优化的工作。此时可重新开启AI会话，进入下一工作过程。

```